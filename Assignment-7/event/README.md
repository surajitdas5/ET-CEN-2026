# To run the project -

1. Clone or download the file
2. Create a .env file with a variable DB_URL with your own connection string.
    > Example: 
    
    > DB_URL=mongodb+srv://username:password@cluster0.pabcde.mongodb.net/cen_event
3. Run `npm install` to install all the dependencies.